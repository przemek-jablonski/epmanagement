<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Easy Project Management 0.1{% endblock %}</title>

            {% block stylesheets %}
                <link rel="stylesheet" href= "{{ asset('bundles/ticket/css/main.css') }}"/>
                <link rel="stylesheet" href= "{{ asset('bundles/ticket/css/event.css') }}"/>
                <link rel="stylesheet" href= "{{ asset('bundles/ticket/css/events.css') }}"/>
                <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" />
            {% endblock %}



{#
        {% block stylesheets %}
                <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" />
            {% stylesheets
                'bundles/ticket/css/event.css'
                'bundles/ticket/css/events.css'
                'bundles/ticket/css/main.css'
                filter='cssrewrite'
            %}
                <link rel="stylesheet" href="{{ asset_url }}" />
            {% endstylesheets %}

        {% endblock %}

        #}

        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>

    {#{% block bootstrap_navbar %}#}
        {##}
            {#forcing bootstrap top page navigation bar (navbar) globally#}
        {##}
        {#<nav class="navbar navbar-inverse">#}
            {#<div class="container-fluid">#}
                {#<div class="navbar-header">#}
                    {#<a class="navbar-brand">easyProjectManagement:</a>#}
                {#</div>  #}
                {#<ul class="nav navbar-nav">#}
                    {#<li><a href="{{ path('ticketcrud_index') }}"><span class="glyphicon glyphicon-home"></span> easyProjectManagement</a></li>#}
                    {#<li><a href="{{ path('ticketcrud_index') }}"><span class="glyphicon glyphicon-list-alt"></span> list</a></li>#}
                    {#<li><a href="#"><span class="glyphicon glyphicon-signal"></span> sort</a></li>#}
                    {#<li><a href="#"><span class="glyphicon glyphicon-search"></span> search</a></li>#}
                    {#<li><a href="#"><span class="glyphicon glyphicon-question-sign"></span> helper</a></li>#}
                {#</ul>#}
                {#<ul class="nav navbar-nav navbar-right">#}
                    {#<li><a href="#"><span class="glyphicon glyphicon-user"></span> profile</a></li>#}
                    {#<li><a href="{{ path('user_register') }}"><span class="glyphicon glyphicon-user"></span> register</a></li>#}
                    {#<li><a href="{{ path('user_login_form') }}"><span class="glyphicon glyphicon-user"></span> login</a></li>#}
                {#</ul>#}
            {#</div>#}
        {#</nav>#}
    {#{% endblock %}#}

    {% block bootstrap_navbar_loop %}

        <nav class="navbar navbar-inverse">
            <div class="container-fluid">

                <ul class="nav navbar-nav">
                    <li><a href="{{ path('ticketcrud_index') }}"><span class="glyphicon glyphicon-home"></span> easyProjectManagement 0.1</a></li>
                    {% if navbarLeft is defined %}
                        {% for element in navbarLeft %}
                            <li><a href={{ path( element.link ) }}><span class="{{ element.glyphicon }}"></span>{{ element.title }}</a></li>
                        {% endfor %}
                    {% endif %}

                </ul>
                <ul class="nav navbar-nav navbar-right">
                    {% if navbarRight is defined %}
                        {% for element in navbarRight %}
                            <li><a href={{ path( element.link ) }}><span class="{{ element.glyphicon }}"></span>{{ element.title }}</a></li>
                        {% endfor %}
                    {% endif %}
                </ul>
            </div>
        </nav>
    {% endblock %}


    {% block bootstrap_flashmessages %}
        {#
            interface for catching and rendering bootstrap flashbars globally
            (if given template is extending this '::base.html.twig')
        #}
            {% if app.session.flashBag.has('flash_success') %}
                <div class="alert alert-success fade in">
                    {% for message in app.session.flashbag.get('flash_success') %}
                        {{ message }}
                    {% endfor %}
                </div>
            {% endif %}

            {% if app.session.flashBag.has('flash_error') %}
                <div class="alert alert-danger fade in">
                    {% for message in app.session.flashbag.get('flash_error') %}
                        {{ message }}
                    {% endfor %}
                </div>
            {% endif %}

            {% if app.session.flashBag.has('flash_warning') %}
                <div class="alert alert-danger fade in">
                    {% for message in app.session.flashbag.get('flash_warning') %}
                        {{ message }}
                    {% endfor %}
                </div>
            {% endif %}
    {% endblock %}


        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
